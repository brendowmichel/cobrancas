# Versao v54 - 2026-06-24

## Resumo

Atualizacao visual da dashboard do Google Apps Script inspirada na referencia enviada pelo usuario.

## Mudancas

- Incrementado `APP_VERSION` para `v54`.
- Tipografia passa a priorizar `GT Walsheim Pro` quando disponivel.
- Adicionado fallback web `Manrope` via Google Fonts.
- Botoes principais/secundarios recebem acento azul-esverdeado suave.
- Tags de status, cadastro e duplicatas passam para formato pill com borda visivel.
- Campos e versao tambem foram ajustados para o mesmo estilo com bordas suaves.
- Mantido tema claro; nao foi aplicado modo escuro.

## Validacao

- Sintaxe validada com `new Function`.
- Integridade conferida: arquivo acima de 2000 linhas e `function isBlankRow_` presente.

